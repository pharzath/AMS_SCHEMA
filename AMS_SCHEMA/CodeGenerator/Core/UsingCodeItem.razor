@using AMS.Model
@using Olive
using @(CodeGen.Module.Microservice.GetModule(Type))@(PostFix.WithWrappers(".",null));
@code {
    [CascadingParameter]
    public CodeGeneratorTemplate CodeGen { get; set; }

    [Parameter]
    public AmsNeo4JMicroserviceModule.ModuleTypeEnum Type { get; set; }

    [Parameter]
    public string PostFix { get; set; }
}
