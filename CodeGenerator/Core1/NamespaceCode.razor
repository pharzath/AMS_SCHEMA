@using AMS.Model
@using Olive
namespace @(CodeGen.Module)@(CodeGen.Prepare(PostFix).WithWrappers(".",null));
@code {

    [CascadingParameter]
    public required CodeGeneratorTemplate CodeGen { get; set; }

    [Parameter]
    public string? PostFix { get; set; }
}
